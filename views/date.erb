<!DOCTYPE html>
       <html>
       <head>
       </head>
       <body>
         <h1><%=Date.today.strftime("%A, %B %d, %Y")%></h1>
           <p>The date is <%=Date.today.strftime("%A, %B %d, %Y")%>.</p>
       </body>
       </html>
